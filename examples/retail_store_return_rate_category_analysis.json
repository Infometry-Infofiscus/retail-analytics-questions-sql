{
  "q_id": 4,
  "difficulty": "Hard",
  "db_type": "Relational (SQL)",
  "domain": "Retail",
  "instruction": "Which stores have a return rate above the company average, and what product categories are driving those returns?",
  "context": "Operations and QA team runs quarterly returns review. Stores with abnormally high return rates flagged for investigation — causes may include product quality issues, staff sales behaviour, or customer expectation misalignment. Report drives coaching conversations with store managers and product team escalations.",
  "metrics_and_aggregation": [
    {
      "kpi_metric_name": "Store Return Rate (%)",
      "aggregation_formula": "ROUND(100.0 * COUNT(CASE WHEN is_return = TRUE THEN 1 END) / COUNT(*), 2) grouped by store_id, last 90 days"
    },
    {
      "kpi_metric_name": "Company Average Return Rate (%)",
      "aggregation_formula": "ROUND(AVG(return_rate_pct), 2) across all stores — used as benchmark"
    },
    {
      "kpi_metric_name": "Category Return Rate (%) per High-Return Store",
      "aggregation_formula": "ROUND(100.0 * COUNT(CASE WHEN is_return = TRUE THEN 1 END) / COUNT(*), 2) grouped by store_id, category"
    },
    {
      "kpi_metric_name": "Category Return Count",
      "aggregation_formula": "COUNT(CASE WHEN is_return = TRUE THEN 1 END) grouped by store_id, category"
    }
  ],
  "chain_of_thought": [
    "Step 1: Goal is to identify stores exceeding company-average return rate and drill into which product categories are the primary return drivers at those stores.",
    "Step 2: Need sales facts (fact_sales) for return/transaction counts, store dimension (dim_store) for store metadata and opening date, product dimension (dim_product) for category, and date dimension (dim_date) for 90-day window.",
    "Step 3: Filter to last 90 days (full_date >= CURRENT_DATE - 90 days). Exclude newly opened stores (opening_date <= CURRENT_DATE - 90 days) to avoid distortion from low-volume locations.",
    "Step 4: CTE 1 (store_return_rates) computes return rate per store. CTE 2 (company_avg) takes AVG of those rates as scalar benchmark. CTE 3 (high_return_stores) filters store_ids exceeding benchmark via CROSS JOIN.",
    "Step 5: Final SELECT joins high_return_stores back to fact_sales + all dims, groups by store + category, orders by store name then category_return_rate_pct DESC to surface worst categories first per store.",
    "Step 6: CROSS JOIN company_avg in final SELECT passes benchmark value into every output row for inline comparison. Edge case: stores with very few transactions may have volatile return rates — volume context (category_transactions) included for that reason.",
    "Step 7: Return rate computed on all transactions (including returns) in denominator — consistent with industry standard. Stores just above average by small margin should be interpreted cautiously alongside transaction volume."
  ],
  "schema_tables": {
    "fact_tables": ["fact_sales"],
    "dimension_tables": ["dim_store", "dim_product", "dim_date"]
  },
  "data_model_layers": {
    "hierarchies": "Store > City > Region, Product > Category",
    "aggregations": "agg_store_return_rate_quarterly, agg_category_return_rate",
    "snapshots": "snap_store_returns_90d"
  },
  "sql": "WITH store_return_rates AS (\n  SELECT\n    s.store_id,\n    COUNT(CASE WHEN s.is_return = TRUE THEN 1 END) AS return_count,\n    COUNT(*) AS total_transactions,\n    ROUND(\n      100.0 * COUNT(CASE WHEN s.is_return = TRUE THEN 1 END) / COUNT(*),\n    2) AS return_rate_pct\n  FROM fact_sales s\n  JOIN dim_date d ON s.date_id = d.date_id\n  WHERE d.full_date >= CURRENT_DATE - INTERVAL '90 days'\n  GROUP BY s.store_id\n),\ncompany_avg AS (\n  SELECT ROUND(AVG(return_rate_pct), 2) AS avg_return_rate\n  FROM store_return_rates\n),\nhigh_return_stores AS (\n  SELECT srr.store_id\n  FROM store_return_rates srr\n  CROSS JOIN company_avg ca\n  WHERE srr.return_rate_pct > ca.avg_return_rate\n)\nSELECT\n  st.store_name,\n  st.city,\n  st.region,\n  p.category,\n  COUNT(CASE WHEN s.is_return = TRUE THEN 1 END) AS category_returns,\n  COUNT(*) AS category_transactions,\n  ROUND(\n    100.0 * COUNT(CASE WHEN s.is_return = TRUE THEN 1 END) / COUNT(*),\n  2) AS category_return_rate_pct,\n  ca.avg_return_rate AS company_avg_return_rate_pct\nFROM fact_sales s\nJOIN dim_store st ON s.store_id = st.store_id\nJOIN dim_product p ON s.product_id = p.product_id\nJOIN dim_date d ON s.date_id = d.date_id\nCROSS JOIN company_avg ca\nWHERE s.store_id IN (SELECT store_id FROM high_return_stores)\n  AND d.full_date >= CURRENT_DATE - INTERVAL '90 days'\n  AND st.opening_date <= CURRENT_DATE - INTERVAL '90 days'\nGROUP BY st.store_name, st.city, st.region, p.category, ca.avg_return_rate\nORDER BY st.store_name, category_return_rate_pct DESC;"
}